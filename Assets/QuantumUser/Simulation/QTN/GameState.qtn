singleton component GameState {
    GamePhase CurrentPhase;
    FP CountdownTimeRemaining;
    PlayerRef LobbyCreator;
    bool CountdownActive;
}

enum GamePhase {
    Lobby,
    Countdown,
    Playing
}